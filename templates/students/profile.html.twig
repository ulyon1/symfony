{% extends 'base.html.twig' %}
{% block body %}
    <dl>
        <dt>First name</dt>
        <dd class="student_profile_first_name">{{ student.firstName }}</dd>
        <dt>Last name</dt>
        <dd class="student_profile_last_name">{{ student.lastName }}</dd>
        <dt>Uuid</dt>
        <dd class="student_profile_id">{{ student.id }}</dd>
    </dl>

{% endblock %}
